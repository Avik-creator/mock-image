# pnpm configuration
# Note: The warning about ignored build scripts for puppeteer and sharp is safe to ignore:
# - We use puppeteer-core (doesn't need build scripts)
# - Images are unoptimized (sharp not needed)
# This warning doesn't affect functionality

